import { Button } from '@mui/material';
import React from 'react';
import Center from '~/renderer/screens/shared/layout/center';

function MainScreen() {
  return (
    <Center width={1} height={1}>
      <Button variant="contained">Button</Button>
    </Center>
  );
}

export default MainScreen;

